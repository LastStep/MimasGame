---
id: R-2026-09-23-T-0012
task: T-0012
project: mimas
role: builder
model: opus
started: 2026-09-23
finished: 2026-09-23
outcome: merged
ladder_result: studio/runs/.ladder/T-0012.json
commits: [ff74634, 3308ad0]
cost_usd: 0
---

# Run: the camera controls

> Opened at the start of the session and appended to as it goes. Not written at the end.

## Log

- Oriented: STATE, the camera code (`CameraDirector`, `CinemachineCameraView`, two fixed vcams), the maps
  (spawns on ±X, centred on the origin), the design's `#camera`. No key conflicts: the Arena only reads
  Escape and Tab.
- Rohan's request plus four answers (free rotation; you always on the left; behind view fixed on your side;
  wheel zoom and V). Wrote F-camera, the spec `docs/specs/2026-09-23-camera.md` and T-0012 (running).
- The asset guard refused two read-only shell commands (an `ls` of the protocols folder and a `grep` of the
  Arena scene / studio config) because their text named protected paths; read them with the Read/Grep tools.
- Wrote `CameraRigMath`, `ArenaCameraRig`, `BoardInputController.PointerOverOverlay`, the `MatchSession` call
  and 10 EditMode tests. Refresh + compile clean; EditMode **24/24** (was 14).
- Wired the scene through the live Editor (unity-editor-mcp): rig on `vcam_Tilted`, `_director`, `_input`,
  `MatchSession._cameraRig`; saved. The scene diff is one component block and one reference.
- Play Mode, practice, open field. Every key driven by queuing real Input System state events (so the rig's
  own polling ran, not a method call), pose read back after each:
  - framed: Side On, pivot (0,0,0), yaw 0, pitch 50, distance 21, camera (0, 16.09, −13.50) = the authored
    pose; pan limit 5.196 (3 tiles).
  - V → Behind You, yaw 90 (looking +X from p1's end).
  - E + W held ~3.3 s → yaw turned ~+300° (90°/s, wrapped), pivot 5.13 from centre against the 5.196 limit.
  - one wheel notch out → distance 21 → 23.
  - Space → Side On, yaw back to 0 the short way, pivot (0,0,0), distance 21, camera at the authored pose.
  - Tab to top-down, E held → the tilted yaw did not move.
  - wheel notch with the pointer over the action bar → `PointerOverOverlay` true, distance unchanged.
- Tuned Behind You from 45°/21 to **55°/25** after projecting arena-4's spawns: at 45°/21 your hero's feet
  on the Arena map land at y≈985 of 1080, behind the action bar (top ≈970); at 55°/25 they land at ≈937.
  Pitch alone gives diminishing returns because the view is centred on the arena (numbers below).
- Rung 0 went red once: Play Mode had written the dynamic font atlases into the six `UI/Fonts` font assets
  (the cache STATE warns about). `git restore MimasClient/Assets/_Game/UI/Fonts` with the Editor out of Play
  Mode; the Editor did not write them back; ladder green.

## What is different now

The camera is yours. Hold Q or E to turn round the arena, W A S D to slide across it (it stops at the
arena's edge, or further if you raise the margin), the wheel to zoom, Space to glide back to the start, and V
to flip between the side-on view — your hero always on the left now, even as seat 2 online — and a view from
behind your end looking at the opponent. Every speed, limit and angle is on the `ArenaCameraRig` component on
`Cameras/vcam_Tilted` in the Arena scene, and changing them in Play Mode takes effect immediately.

## Deviations from the plan

- Behind You starts at 55° / 25 rather than the spec's 45° / 21, from the arena-4 projection above. Both are
  Inspector values.

## Ladder

| Rung | Check | Result | Evidence |
|---|---|---|---|
| 0 | asset safety | green | after restoring the font cache (see log) |
| 1 | core build | green | 4.7 s |
| 2 | core tests | green | 461 passed |
| 5 | server integration | green | 58 passed |
| — | Unity compile | clean | `recompile_status` completed, `compilationFailed: false`; console errors 0 from the game |
| — | EditMode | green | 24/24 (10 new `CameraRigMathTests`) |

## What did not work

- The `capture_game_view` MCP command refuses a `..` path, so it cannot write into `artifacts/`; captures
  used `ScreenCapture.CaptureScreenshot` from `eval` instead (native 1920×1080). Its refusal is the one
  console error of the session.
- The first Play Mode entry came up with no heroes (the known first-Arena-load quirk); a scene reload fixed it.
- On the Arena map the Behind You view puts your hero only ~35 px above the action bar. Centring on the
  arena is what Rohan asked for; a small look-ahead towards your end would lift it, if he wants one.

## Evidence for a human

- `artifacts/t0012/1-side-on.png` — the default view, your hero (red) left.
- `artifacts/t0012/2-behind-you.png` — after V: your hero bottom, the opponent across the board.
- `artifacts/t0012/3-rotated-panned-zoomed.png` — after E + W held and one notch out: turned, at the pan
  limit, zoomed.
- Arena-4 framing numbers (screen y of 1080; action bar top ≈970):

  | Behind You | near feet | near head | far spawn | open-field near |
  |---|---|---|---|---|
  | 45° / 21 | 985 | 913 | 252 | 847 |
  | 55° / 25 | 937 | 898 | 244 | 823 |
  | 60° / 26 | 932 | 905 | 233 | 822 |

To see it: open the Arena scene, press Play, press V, hold Q/E and W A S D, roll the wheel, press Space.

## Files changed

- Presentation: `Cameras/CameraRigMath.cs` (new, the maths), `Cameras/ArenaCameraRig.cs` (new, the rig),
  `Input/BoardInputController.cs` (`PointerOverOverlay`), `Match/MatchSession.cs` (`_cameraRig` + one
  `Frame` call), `README.md`.
- Tests: `Tests/EditMode/CameraRigMathTests.cs` (new, 10 cases).
- Scene: `Arena` — the rig on `vcam_Tilted`, three references.
- Docs: design `#camera`, ADR-038, spec `docs/specs/2026-09-23-camera.md`.
- Studio: F-camera, T-0012, this report, STATE.

## Left for next time

- Rohan plays it and tunes: rotate/pan/zoom speeds, pan margin, smoothing, both framings.
- Offered and not chosen on 23 Sep: top-down on the same rig with a real toggle key, middle-drag pan.
- Possible: remember the last view across a round's scene reload (today each round opens on the default);
  a look-ahead for Behind You on the Arena map; hold-Shift to pan faster.
- The Web build was not rebuilt this session; the rig is presentation-only C#, no assets.

## Verifier

> Appended by the verifier, in a fresh context. Pass or fail, with reasons.

**Verdict: PASS.** 24 Sep 2026, verify round at HEAD `0a46b79`. The diff read was `ff74634`, `3308ad0` and `a0c5a68`. None of the camera files, the test file, or `BoardInputController.cs` has changed since `ff74634`, and the `MatchSession` call is still at line 628.

**Done-when, one by one**

1. **Q/E.** Implemented at `ArenaCameraRig.cs:217-219`: E minus Q, times `_rotateSpeed` in degrees per second (field at `:50`), added to the yaw target with no snapping. Increasing the yaw turns the camera clockwise seen from above, so E is clockwise and Q counter-clockwise, which matches spec §2 and design `#camera`. The run report logs a readback of ~300° after 3.3 s held, and capture 3 shows the turn.
2. **W A S D.**
   - The pan follows the camera's current yaw (`:221-229`, `CameraRigMath.PanDirection`) at `_panSpeed`, and the zoom scaling follows spec §2.
   - The pan target is clamped every frame (`:233`) to `PanLimitRadius` (`:129`): the furthest tile centre plus `_panLimitTiles × board.Spacing`. `board.Spacing` is `HexLayout.TileWidth`, the distance between tile centres (`BoardView.cs:96`).
   - The pivot only moves by lerping between points inside that disc, so it never leaves the disc.
   - EditMode tests cover the clamp and the extent.
3. **Space, V and the default view.** Space calls `GoTo(_defaultView)` and V calls `GoTo(the other view)` (`:214-215`). `GoTo` (`:167-178`) centres the view on the arena and sets that view's pitch and distance, then smoothing glides there. `_defaultView` is an Inspector field of type `ArenaView { SideOn, BehindYou }`.
4. **Views per seat.**
   - `Frame` chooses home and away spawns by seat (`:155-158`). `MatchSession.cs:628` passes `LocalPlayer`, which is the driver's `youAre` online and `Local` in practice.
   - `ViewYaw_SideOn_PutsYourSpawnOnTheLeft(0|1)` and `ViewYaw_BehindYou_LooksFromYourSpawnAtTheirs(0|1)` both pass in `artifacts/verify-round/editmode.xml`.
   - I checked the geometry by hand for both seats. Seat 1 gets yaw 0 and seat 2 yaw −180, so home is on the left for both.
5. **Wheel.** Zoom is clamped to `_minDistance` and `_maxDistance` (`:236-241`, `:252-257`). The wheel returns early when `BoardInputController.PointerOverOverlay` is true (`BoardInputController.cs:104`). That flag comes from the HUD's `panel.Pick` blocker (`MatchHudView.cs:1267`), which still exists at HEAD. The run report logs the check over the action bar.
6. **Top-down, Tab, stopped time.**
   - The rig writes only its own transform (`:268-273`) and reads keys only while `CameraDirector.ActiveView` is its own view (`:198`, `:244-248`).
   - `CameraDirector` is untouched, and no other script binds Space, V, Q, E, W, A, S or D (grepped at HEAD).
   - Motion uses `Time.unscaledDeltaTime` (`:197`). The project has no Input System settings asset, so input is polled in the default dynamic update and still works when time is stopped.
7. **Console, tests, captures.**
   - The lead's batch EditMode run at HEAD compiled and ran 40 tests with 0 failures. Neither Editor log has a `warning CS` or `error CS` line.
   - EditMode was 14 tests before (13 in `_Game` plus the Addressables stub) and is 24 after. The only test file in the diff is the new `CameraRigMathTests.cs` with 10 cases, so no test was softened.
   - The three captures exist in `artifacts/t0012/` and are named in this report. I looked at each: 1 has your hero (red) on the left; 2 has you at the bottom looking at the opponent; 3 is turned, sitting on the pan rim, and zoomed.
8. **Docs.** Design `#camera` (`index.html:984`), ADR-038 (`decisions.md:45`), the Presentation README, STATE (lines 54-61 and 156) and this report are all present. What they say matches the code.

**Other checks**

- **Ladder.** `studio/runs/.ladder/T-0012.json` is green on the requested rungs 0, 1, 2 and 5 at `0a46b79`, with 473 Core and 60 server tests. Both ratchets are ok.
- **Asset safety, checked by hand.** At HEAD, rung 0 checked only the two dirty studio files, not T-0012's commits. So I checked the commits by hand: every protected path they touch is in `allows_assets`. That is 3 new `.cs.meta` files in Unity 6 two-line form, `Arena.unity` and `docs/design/index.html`.
- **Scene diff.** One `ArenaCameraRig` block on `vcam_Tilted`, one `m_Component` entry and `MatchSession._cameraRig`. `_director` points to CameraDirector and `_input` to BoardInput.
- **Nothing else.** The diff does not touch the ledger, Core, the server or the wire.
- **Scope.** Nothing in the diff falls outside F-camera, and nothing from its "Not this feature" list was built.
- **Plan deviation.** Behind You at 55°/25 instead of 45°/21 is declared, and it is a tuning value.

**Notes (none of them block the pass)**

- **Framings do not change the view immediately.** Changes to `_sideOn`, `_behindYou` or `_defaultView` in Play Mode take effect at the next Space or V (they are read in `GoTo`, `:171-177`). Speeds, limits and smoothing change every frame. So "takes effect immediately" (line 55 of this report) is true of those, not of framings. For Rohan's tuning: press Space after changing a framing. Owner: builder, small, doc only.
- **Not seen running.** Stopped time and seat 2 were checked by reading code and by the EditMode tests, which is what the done-when names. Neither was seen in Play Mode.
- **Step 4 is Rohan's.** He plays the camera and tunes it (STATE item 5). This is outstanding and not required for this pass.

**Ledger:** `studio/ledger.json` has no criterion for the camera or M4, so there is no row to flip.
