---
id: R-2026-09-24-T-0014
task: T-0014
project: mimas
role: builder
model: opus
started: 2026-09-24 11:03
finished: 2026-09-24 11:50
outcome: merged
ladder_result: studio/runs/.ladder/T-0014.json
commits: [7932513, eacc750, 90e4475]
cost_usd: 0
---

# Run: the lobby and the room in ink

> Opened at the start of the session and appended to as it goes. Not written at the end.

## Log

- 11:03 Oriented: STATE, T-0014 (lane full, plan = `docs/specs/2026-09-23-hud-restyle.md` §8–§14), F-hud-restyle,
  `docs/ui/lobby.md`, `docs/ui/language.md`, the protocols, design `#presentation`. Role builder, task T-0014, on
  `main`. **Approval:** the task sat at `plan` waiting for Rohan to see T-0013 built; Rohan said "lets execute
  t-0014" in chat on 24 Sep, so it is running on his word. Whether he has looked at T-0013 first is not recorded.
- `git status`: `studio/runs/events.ndjson` modified by the studio's hook log, not mine — left alone.
- The Editor was closed at session start and unity-mcp was not connected; `unity open MimasClient` started.
- The asset guard refused a read-only grep whose text named the presets `.asset`; read with the Grep tool
  instead (the known false positive in STATE).
- Design `#presentation` → Lobby does not yet say "two seats facing" (T-0013 left it to this task): commit 1.
- **Commit 1 `7932513`** — design `#presentation` → Lobby points at `docs/ui/lobby.md` (proposed): you always left,
  the other seat only name and readiness, gear as one tile per preset.
- Rendered the two canvas boards to PNG (`artifacts/t0014/boards/M-Lobby.png`, `M-Room-Facing.png`) with T-0013's
  `render-boards.mjs`.
- Built: `UI/RoomLayout.cs` (`RoomLayout.LeftSeat/RightSeat/Other/LastResultLine`, `RoomSeat`, `OtherSeatText`,
  `RoomChoice`), `Tests/EditMode/RoomLayoutTests.cs` (the four of §9 plus two for the result line), `Lobby.uxml`
  and `Lobby.uss` rewritten to `lobby.md` §2–§3, `LobbyView.cs` (preset tiles and lineage rows built in code under
  `room.gear.tiles` / `room.god.rows` as `room.preset[i]` / `room.lineage[i]`, the Examine convention for rows
  that repeat; you-left seats; the null guard), `Ramps` + `BothEndsVertical` (the divider), `LobbyWash`, `Swatch`;
  `Theme.uss` + `--mimas-you-07`, `--mimas-bone-22`, `--mimas-bone-30`, `--mimas-ink-25`, `--mimas-tracking-title`,
  `--mimas-tracking-code` (values from `lobby.md`). The Editor made both `.meta` (the two declared).
- **Outside the four files the spec lists** (each small, client only, no wire): the EditMode test asmdef references
  `Mimas.Client.UI` (RoomLayout lives there, per the spec); `MatchResult` carries the series score and
  `MatchSession.BackToLobby` fills it, because `lb.last` shows "SERIES 2 – 1" and nothing carried it;
  `ExamineModelBuilder.EmblemOf` internal → public so the room's rows draw the plate's emblem rather than a copy of
  the mapping; `FontSpacing` (below).
- **The book over the canvas (rule 3):** the canvas draws per-item glyphs on the gear tiles (longbow as a sword,
  flintlock as the shot arc, blink boots as the blink mark); `lobby.md` says "the weapon's and the boots' slot
  glyphs", so every tile shows the sword and the boot and the names tell them apart. Item icon art is already an
  art-slot follow-up.
- **Found: the washes drew half as bright again as the canvas.** The project is linear (`m_ActiveColorSpace: 1`), so
  a translucent colour blends in linear space. The lobby's wash is therefore mixed onto the ink in gamma values and
  drawn opaque (`Ramps.LobbyWash(hues, ground)`) — it now matches the board. **The same effect on every translucent
  token, not fixed here:** Ready's fill measured rgb(42, 90, 88) against the board's (31, 56, 60); the selected
  tile (34, 77, 74) against (22, 41, 44). It is the HUD's too (T-0013 uses the same tokens). This Unity has no
  `PanelSettings.forceGammaRendering`. Follow-up for Rohan, not a T-0014 change.
- **Found: tracked capitals lost their spacing on kerned pairs in the lobby** ("YOU", "LE AVE ROO M") and the series
  band read "V I C TORY" again. `FontSpacing` cleared the kerning flag once, when the HUD bound; the lobby never
  called it, and a dynamic font brings a glyph's pairs back flagged when the glyph is first drawn. Fixed:
  `FontSpacing` adds every tracked character (capitals, digits, the punctuation printed) before clearing, and
  `LobbyView` repairs at bind. Crops before/after: `artifacts/t0014/crop-you*.png`, `crop-leave*.png`; the band
  after: `shots/extra-series-band-1080.png`.
- "MIMAS" and the room code sat ~14px right of centre: UI Toolkit adds no tracking after the last letter, so the
  canvas's balancing padding was dropped.
- **Played in the Editor against the local server** (`dotnet run --project server/Mimas.Server`), the other seat a
  Node guest on the real protocol (`artifacts/t0014/seat.mjs`): Create room → waiting; they join and ready; you pick
  a tile and a god; Ready locks all seven choices (`NOT READY`, "Waiting for your opponent…"), a disabled tile refuses
  the press, un-ready keeps the tile; Leave; join their room by a lower-case code as seat 2 (you still left); a room
  match starts (the Arena loads), they resign, **VICTORY · SERIES 1 – 0 · OPPONENT RESIGNED**, Back to room → the
  after-result line, Leave → `lb.last` "VICTORY VS GUEST-4417 · SERIES 1 – 0"; Play vs bot → RANDOM BOT · READY →
  Ready → the Arena. Copy code → "Code copied."; Copy link → "Link copied." (the code alone in the Editor, which has
  no page URL, as before). Console **0 errors, 0 warnings** throughout.
- Captures (Editor, 1920×1080 and 1280×720): `artifacts/t0014/shots/1-lobby-last-result-*`, `2-room-waiting-*`,
  `3-room-both-*`, `4-room-seat2-*`, `5-room-after-result-*`; extras `extra-room-ready-720`, `extra-room-bot-1080`,
  `extra-series-band-1080`, `extra-bot-match-1080`. Scripts: `capture-room.sh`, `capture-after.sh`.
- `dotnet build Mimas.slnx` 0 errors; **Core 473, server 60** (unchanged: T-0014 touches neither).
- Closed the Editor (`File/Exit`); it wrote the dynamic font atlases into the three font assets on the way out —
  `git restore MimasClient/Assets/_Game/UI/Fonts` (STATE). **EditMode, batch: 40 tests, 0 failures** (was 34; the six
  `RoomLayoutTests` listed by name in `artifacts/editmode.xml`), log `artifacts/t0014/editmode-batch.log`.
- **Web build** (`WebBuild.Build`, exit 0): **13,120,694 → 13,117,682 B** (`du -sb Build/Web`; 12.51 MiB, under the
  13 MB ratchet); `.data.br` 4,875,237 → 4,865,854, `.wasm.br` 8,126,944 → 8,133,310. Font caches restored after it.
- **Smoke, local server serving the new build, 1280×720, Play vs bot → Ready → a match** (`--expect "\[MatchSession\]
  online"`): Chromium exit 0 `boot 1329 ms`, WebKit exit 0 `boot 1248 ms`, Firefox exit 0 `boot 1638 ms`, 12.5 MB each,
  no pageerror, no console.error. The lobby moved its buttons: at 1280×720 Play vs bot is **640,357** and Ready
  **640,605** (T-0013's 640,524 for Ready would now hit nothing). Shots `artifacts/t0014/smoke-*/`.
- **Seat 2 from the browser** (item 4): the Node guest opened a ready room, the browser followed `?room=<code>`, was
  drawn on the left, pressed Ready and the match started — at 1920×1080 (`seat2-1920x1080/room-seat2.png`) and
  1280×720 (`seat2-1280x720/room-seat2.png`), exit 0 both, no console errors.
- **Ladder `--task T-0014`: GREEN 4/4** (rung 0 green over 16 paths, 1 green, 2 green 473, 5 green 60), on the final
  code with the client change uncommitted — `studio/runs/.ladder/T-0014.json`.
- **Commit 2 `eacc750`** — client. **Commit 3 `90e4475`** — `lobby.md` built with its ids and two drifts,
  `language.md` (the T-0014 tokens, the Lobby void), `architecture.md`.

## What is different now

A friend who opens the link sees one dark column — MIMAS, the last result in teal or red with the series score,
their name, Play vs bot, Create room, a code and Join — over three faint washes in the gods' colours. In a room,
they are on the left in teal whichever seat they hold: their name, four gear tiles, three gods as rows with a
coloured square and emblem. The other player is on the right in red with only a name and READY or CHOOSING (a
bot reads RANDOM BOT · READY; an empty seat WAITING FOR A PLAYER). The code sits large at the top, Ready at the
foot. Ready locks the tiles and the gods; nothing about how a room works changed.

## Definition of done (spec §14, T-0014)

- [x] EditMode ≥ 34 + 4: **40** (six new, none softened); Core **473** and server **60** unchanged.
- [x] Every id of `lobby.md` §2–§3 is an element name: in `Lobby.uxml` `lb.wash lb.title lb.last lb.name.caption
      lb.name lb.play-bot lb.create lb.join.code lb.join lb.status lb.server room.code.caption room.code
      room.copy-link room.copy-code room.you.caption room.you.name room.gear.caption room.god.caption room.divider
      room.them.caption room.them.name room.them.state room.them.note room.ready room.leave room.status`; built by
      `LobbyView` under `room.gear.tiles` / `room.god.rows`: `room.preset[i]`, `room.lineage[i]` (read back live by
      `artifacts/t0014/state.cs`). `Lobby.uss` has only `--mimas-*` (grep: no `#`, no `rgb`, no `--lobby-`), radius 0
      except the ready dot (language §4: dots).
- [x] No `DropdownField` in the lobby; one tile per preset; the lineage rows are elements.
- [x] Captures in `artifacts/t0014/`: items 1–5 at both sizes (`shots/1-…` to `5-…`), seat 2 from the browser
      (`seat2-*/room-seat2.png`). Boards to hold them against: `artifacts/t0014/boards/`.
- [x] Console clean (0 / 0); Web build 12.51 MiB ≤ 13 MB; smoke green on three engines; Play vs bot and the room
      code both start a match (Editor and browser).
- [x] Docs of §10 (design `#presentation`, the UI book, architecture); STATE; this report; T-0014 at `verify`.

## What did not work, or is not done

- **Translucent colours draw ~60% brighter than the canvas** everywhere (linear colour space), the HUD included.
  Measured, noted as a drift, not fixed — it changes the look of every screen, so it is Rohan's call. The lobby's
  wash is the one place I compensated, because there it made the whole screen visibly wrong.
- **I reached outside the spec's file list** four times (the test asmdef, `MatchResult` + `BackToLobby`, `EmblemOf`
  public, `FontSpacing`). Each is above with its reason; none touches Core, the wire or the server.
- **`FontSpacing` now rasterises ~60 characters per font at first bind** so no glyph arrives later. Boot on Chromium
  was 1329 ms against T-0013's 672 ms on the same machine — but this run's boot is also the first after a cold server,
  and WebKit (1248 vs 1130) and Firefox (1638 vs 1472) moved less. Not measured apart; `?perf=1` would say.
- The gear tiles all show the same two glyphs (the book's slot glyphs); the canvas had per-item placeholders.
- The four room captures in the Editor use a Node guest (`seat.mjs`) as the other player, not a second browser.
- Not verified by anyone but me. A verifier in a fresh context and Rohan's eyes are next.

## Cost

Wall-clock 11:03 – 11:50 (about 50 minutes). Session cost not visible to me: `cost_usd` left at 0.

## Next

1. **Rohan looks at the lobby and the room** (the captures above, or `https://…/?room=` against a local build) and
   says whether the translucent fills should match the canvas exactly (a follow-up across every screen).
2. **A verifier** for T-0014 in a fresh context.
3. Deploy carries T-0010 … T-0014 together.
