---
id: R-2026-09-21-T-0008
task: T-0008
project: mimas
role: builder
model: opus
started: 2026-09-21 (session opened; wall-clock per the environment)
finished:
outcome: running
ladder_result: studio/runs/.ladder/T-0008.json
commits: []
cost_usd: 0
---

# Run: Finish M2 in the browser — rematch in the room, Mimas Web template, Copy code, blocker shown, origin check, multi-browser smoke

> Opened at the start of the session and appended to as it goes. Not written at the end.

## What is different now

_(filled as the session goes)_

## Deviations from the plan

_(first, if any)_

## Ladder

| Rung | Check | Result | Evidence |
|---|---|---|---|
| 0–1 | `dotnet build Mimas.slnx` (baseline, before any change) | green | 0 Errors, 4 warnings (pre-existing xUnit analyser warnings) |

## Session log

- **Read first:** `studio/STATE.md`, `studio/tasks/T-0008-m2-browser-finish.md`, `studio/plans/P-T-0008.md`,
  `studio/features/F-m2-browser-finish.md`, `docs/specs/2026-09-21-m2-browser-finish.md` (whole file).
- **Ground:** branch `main`, clean but for `studio/runs/events.ndjson` (studio bookkeeping, not mine).
- **Unity Editor:** `unity status --format json` → `STATUS_NO_INSTANCES`. Server half (§3–§4) runs first
  and needs no Editor, exactly as spec §0 rule 3 orders.
- **Asset-guard false positive (read-only):** `ls .claude/protocols/ && find . -name run-report.md -not -path "./Library/*"`
  was blocked because the command *text* names `.claude/**` and `**/Library/**`. Nothing was written.
  That is the ninth entry in STATE's list of read-only false positives; used Glob/Read instead.

## What did not work

_(to fill)_

## Evidence for a human

_(to fill)_

## Files changed

_(to fill)_

## Left for next time

_(to fill)_

## Verifier

> Appended by the verifier, in a fresh context. Pass or fail, with reasons.
